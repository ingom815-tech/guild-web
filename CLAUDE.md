# guild-web — 프라시아 전기 결사 관리 시스템 (신규 웹)

기존 Streamlit 앱(`C:\Users\82107\고니`)을 정적 HTML/CSS/JS + Supabase Edge Functions로 이식한 프로젝트. 같은 Supabase DB(서울 리전)를 기존 Streamlit 앱과 공유한다. **핵심 기능 이식은 완료됐고 1차 배포까지 마친 운영 단계.**

## 배포 상태 (2026-07-19 기준)

- **실서비스 URL**: https://ingom815-tech.github.io/guild-web/ (GitHub Pages)
- **저장소**: https://github.com/ingom815-tech/guild-web (public, 브랜치 master)
- ⚠️ **push = 실서비스 자동 반영** (Pages가 1~2분 내 재배포). 커밋/푸시는 반드시 사용자 확인 후에만.
- 결사원들에게 공개된 상태이므로 실DB 테스트 금지 — 테스트가 필요하면 사전에 사용자와 원복 계획을 합의할 것.

## 아키텍처 핵심

- **인증**: Supabase Auth 미사용. 자체 세션 토큰(`user_sessions` 테이블, `X-Session-Token` 헤더). 비밀번호는 레거시 SHA256 → 로그인 시 bcrypt로 점진 업그레이드.
- **보안**: 전 테이블 RLS 활성화 + 정책 0개(완전 차단). 읽기·쓰기 전부 Edge Function(service_role) 경유. anon 키는 `apikey` 헤더로만(공개 전제 설계 — 저장소에 있어도 안전). 전투력 등 민감 값은 서버가 운영진 세션에만 응답에 포함.
- **Edge Function**: 로컬 Deno/Docker 없음 → 코드 수정 시 로컬 파일(source of truth) 수정 → 사용자에게 전체 코드 출력(채팅) 또는 파일 복사 안내 → 사용자가 Supabase Dashboard "Via Editor"에 붙여넣어 Deploy. 함수마다 헬퍼를 중복한 **자체완결 단일 파일** 구성(의도적). "Verify JWT with legacy secret"는 항상 OFF. ⚠️ SQL 파일을 함수 편집기에 붙여넣는 실수 주의(파싱 오류 남).
- **원자성 필요한 쓰기**: SECURITY DEFINER RPC(`supabase/migrations/`) — anon/authenticated EXECUTE REVOKE. 사용자가 SQL Editor에서 실행.
- **캐시버스팅**: 정적 파일 수정 시 `index.html`의 `?v=N`을 반드시 +1 (현재 v=28).
- **로컬 미리보기**: `_devserver.ps1` → `http://localhost:8899`. 검증은 목 API(`Api.xxx = () => Promise.resolve(...)` 오버라이드)로 브라우저에서.
- **DB 값 표기는 전부 한글**: 등급(희귀/영웅/전설/신화/절대자), 자산(현금/다이아), 방향(입금/출금), 신청 상태(대기/확정/반려/취소), 역할(결사원/운영진/관리자), 재고(재고). 시간은 KST 벽시계값(timestamp without tz) — 비교 시 "문자열을 UTC로 해석한 epoch vs Date.now()+9h" 관례. 단 `guild_transactions.created_at`은 timestamptz(UTC 저장)라 날짜 필터 시 시간대 주의.
- **이미지**: Supabase Storage 버킷 `screenshots`(public, 자동 생성). 클라 canvas 1280px/JPEG q0.82 전처리 → base64 전송 → 서버 업로드 → URL JSON 배열을 `power_img_url`/`status_check_img_url`에 저장. 레거시 base64(data:) 값 하위호환. members 목록 조회는 이미지 블랍 절대 select 금지(유무 플래그만) — 전 회원 base64를 한 번에 읽으면 함수가 죽는다.

## 절대 규칙

- **커밋/푸시는 반드시 사용자 확인 후에만.** 푸시는 곧 실서비스 배포다.
- **참여점수 계산 로직 변경 금지**: `recalc_participation_scores` RPC 공식(활동 로그 수 = 활동컬럼, participation_score = 합계×100, participation_rate = 참석 세션/전체 세션×100, contribution_score = round(참여×0.7+전투력×0.3)) 원본 그대로 유지. 새 지표는 항상 별도 계산.
- **긴급 데이/나이트 분류**: `!긴급` 로그만. 데이 = 09:00:00~17:59:59, 나이트 = 18:00:00~익일 08:59:59 (18:00 정각=나이트, 09:00 정각=데이). 참여점수에 미합산(참고 지표). 조 변경: 최초 선택=현재 시즌 즉시, 변경=다음 시즌부터.
- **공금(다이아/현금) 정산 규칙**:
  - **다이아 = 룻자(운영진) 계좌로만** 입금. 분배 승인 UI에서 룻자를 선택(운영진/관리자 목록, 아이템 룻자 닉 일치 시 자동 선택), 다이아 입력 + 룻자 미선택이면 승인 차단. 수령자 폴백 금지.
  - **계좌 통합 별칭**: `app_settings.dia_account_alias` = `{"크앙":"admin"}` (곰형→관리자). 곰형을 선택해도 실입금은 관리자 계좌. 나머지 운영진은 각자 계좌. finalize(Edge)와 분배취소(0010 RPC)가 같은 별칭 적용, 분배취소는 실제 입금 거래(ref_id) 기준 역전이 1순위.
  - **현금 = 무조건 결사 금고**(guild_treasury, 관리자 관리).
  - **공금 "이력 삭제" = 그 거래를 잔액까지 역전**시키는 취소 기능. 기록 청소 용도로 쓰면 잔액이 무너진다 (7/19 사고 원인).
- **시안 = 디자인 명세**: 색상/레이아웃/카드 구조는 시안 그대로. 데이터 로직 변경은 명시된 것만.
  - 전체 구조: `C:\Users\82107\고니\결사관리_확정구조_시안_v5.html`
  - 대시보드: `guild-web/대시보드_최종통합_시안_v2.html`
  - 아퀴 목록: `C:\Users\82107\고니\아퀴정보목록_개선시안_v2.html`
- **신화 아퀴룬 직업별 매핑** (gamedata.js MYTHIC_RUNES — 대시보드 표 컬럼명은 섹션 중립 "신화 1번/2번"):
  - 향사수: 향연의 덫(A3/PVP), 관통하는 화살(B4/지원) · 집행관: 맹렬한 돌격(C3/PVE), 신의 방패(A3/PVP)
  - 야만투사: 신수의 발톱(C3/PVE), 2번 없음 · 환영검사: 암기 투척(A6/PVP), 환영검 투척(C4/PVE)
  - 태양감시자: 점멸 습격(A6/PVP), 황금률의 파도(B4/지원) · 주문각인사: 유성 낙하(A4/PVP), 고양의 영역(B3/지원)
  - 심연추방자: 휘몰아치는 힘(A6/PVP), 심연의 등불(C6/PVE)
  - 판정 = `:m` 등급 저장분만(M1/M2 레거시 포함). 신화 데이터는 신규 오픈 후 수집 예정. 아퀴 뷰는 DB 저장 섹션(A/B/C) 그대로 렌더링.
- **아퀴 매핑 단일 출처**: 원본 app.py:728 `AQUI_ITEMS` 상수의 이식본인 `js/gamedata.js` (DB 테이블 아님 — 새 매핑 테이블 생성 금지 방침). 야만투사는 25개 슬롯 매핑 없음(코드 fallback이 정상). Edge Function distribution의 AQUI_NAME_TO_ID도 같은 데이터의 사본.

## 완료된 단계 (커밋)

| 커밋 | 내용 |
|---|---|
| a45a118 | 1~3단계: 로그인, 재고/드랍 등록(로그 파서+장바구니), 결사원 CRUD, 공금 관리 |
| e1a8594 | 4단계: 대시보드 |
| 718bc76 | 5~6단계: 분배 신청(기간+자격+자동확정), 장비/아퀴 편집, 대시보드 리팩토링(시안 v2) |
| 99ac4e2 | 신화 아퀴룬 보유자만 표시 |
| 98918ca | 7단계: 참여율 관리 + 긴급 데이/나이트 지표 (점수 불변 diff 0건 검증) |
| aededbb | 8단계: 회원가입 + 내정보 5서브탭 + Storage 이미지 전환 |
| 78dd497 | 결사원 관리 3뷰(스샷 보기/장비표/아퀴표) + members 이미지 분리 조회 |
| 94b26a9 | 9단계: 분배 후반부(현황/결과/이력, RPC 0008) + 분배 상위탭/하위4탭 + 아퀴 목록 시안(사람별/룬별, 미보유 탐색·참여점수 정렬·직업별 룬 그리드) + 아퀴 매핑 3직업 비교 |
| 03d528f | 분배 승인 다이아 룻자 선택제 (푸시됨) |
| 2249347 | 다이아 별칭 곰형→관리자 (0010) — **로컬만, 푸시 대기** |

결과 탭 흐름: 확정 대기에서 다이아/현금 입력 + 룻자 선택 → "나감 처리(승인)" = 즉시 finalize(이력+재고차감+장비/아퀴 갱신+공금 입금). 확정명단 단계 없음. 되돌리기 = 이력 탭 분배취소(관리자). mark_dispatched/undispatch 액션은 호환용으로만 존재.

배포된 Edge Functions(12개): login, logout, change-password, inventory, item-master, members, treasury, dashboard, distribution, participation, profile, register. 마이그레이션: 0001(RLS)~0008(분배 RPC), 0010(다이아 별칭+분배취소 개선 — 0009는 결번/폐기).

## 진행 중 / 다음 할 일

- **⚠️ 사용자 실행 확인 필요 (2026-07-19 마지막 세션에서 안내했으나 완료 미확인)**:
  1. 곰형→관리자 다이아 잔액 통합 SQL (기대: 관리자 205,460 / 세마형 2,720 / 취중힐담 1,360, 곰형 계좌 삭제)
  2. 0010 마이그레이션 실행 (별칭 등록 + cancel_finalized_distribution 교체)
  3. distribution 함수 재배포 (별칭 적용 버전 — 코드는 채팅으로 전달됨)
  4. 프론트 푸시 (커밋 2249347 — 사용자 "푸시해줘" 확인 후)
- **7/19 테스트 데이터 원복 완료 내역** (참고): 테스트 분배 이력 143/144 삭제, 테스트 공금 거래 삭제, 금고 1,568,500 고정, 리카누나/샤드 다이아 계좌 삭제, 유찰 카운트 -2 보정(테스트 마감 2회분), 4~5월 잔존 신청 31건 일괄 취소. 별빛 심연석 재고 2개는 미복원(사용자가 재고 재정리 예정).
- **재고 정리**: 사용자가 직접 예정 (별빛 심연석 수량 등 이상값 포함). 대량 정리 필요 시 SQL 지원.
- **실전 분배 E2E**: 다음 분배 기간에 소량으로 신청→확정→승인(다이아/현금+룻자)→공금 확인→분배취소 원복 검증.
- **8단계 E2E**: 가입 신청→승인→새 계정 로그인→스샷 업로드(Storage 버킷 자동 생성 실전 확인).
- **미뤄둔 부가 기능**: 엑셀 업로드(결사원/참여율), 과거 닉네임 등록 UI, 계좌 DB, 관리자 참여 편집 탭, 전시즌 이월 블렌딩, 규정(guild_regulations) 편집 화면.
- 참고: Supabase/Neon 비밀번호 로테이션 권고했으나 확인 안 됨. 고니 저장소의 CLAUDE.md 갱신 커밋(77490c2)은 로컬만(푸시 시 Streamlit 재배포되므로 보류).
