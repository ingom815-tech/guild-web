# guild-web — 프라시아 전기 결사 관리 시스템 (신규 웹)

기존 Streamlit 앱(`C:\Users\82107\고니`)을 정적 HTML/CSS/JS + Supabase Edge Functions로 이식한 프로젝트. 같은 Supabase DB(서울 리전)를 기존 Streamlit 앱과 공유한다. **핵심 기능 이식 완료 + 1차 배포 후 운영 중이며, UI 전면 개편(공금/로그인/내정보/모바일)과 관리자 도구(로그 관리)까지 진행된 상태.**

## 배포 상태 (2026-07-19 기준)

- **실서비스 URL**: https://ingom815-tech.github.io/guild-web/ (GitHub Pages)
- **저장소**: https://github.com/ingom815-tech/guild-web (public, 브랜치 master)
- ⚠️ **push = 실서비스 자동 반영** (Pages가 1~2분 내 재배포). 커밋/푸시는 반드시 사용자 확인 후에만.
- 결사원들에게 공개된 상태이므로 실DB 테스트 금지 — 테스트가 필요하면 사전에 사용자와 원복 계획을 합의할 것.
- 배포된 프론트 = **v=58** (커밋 표의 마지막 행). 로컬 미푸시 작업 없음.

## 아키텍처 핵심

- **인증**: Supabase Auth 미사용. 자체 세션 토큰(`user_sessions`, `X-Session-Token` 헤더). 비밀번호 레거시 SHA256 → 로그인 시 bcrypt 점진 업그레이드.
- **보안**: 전 테이블 RLS + 정책 0개(완전 차단). 읽기·쓰기 전부 Edge Function(service_role) 경유. anon 키는 공개 전제 설계. 역할 노출: `body.staff`(운영진+) / `body.admin`(관리자) 클래스 + `.staff-only`/`.admin-only` CSS. 단 **서버 권한은 화면 노출과 별개** — 삭제류 API는 운영진도 호출 가능(참석 보정만 서버에서 관리자 전용).
- **Edge Function**: 로컬 Deno 없음 → 로컬 파일(source of truth) 수정 → **전체 코드를 채팅으로 출력**(사용자 선호 — 파일 복사 안내 아님) → 사용자가 Dashboard "Via Editor"에 붙여넣어 Deploy. 함수마다 자체완결 단일 파일. "Verify JWT" OFF.
- **원자성 쓰기**: SECURITY DEFINER RPC(`supabase/migrations/`) — 사용자가 SQL Editor에서 실행. 0001~0008, 0010 적용됨(0009 결번).
- **캐시버스팅**: 정적 파일 수정 시 `index.html`의 `?v=N` 전체 +1 (bash sed `s/?v=N/?v=N+1/g`).
- **⚠️ 한글 파일 인코딩**: 저장소 파일은 BOM 없는 UTF-8. **PowerShell로 파일 내용을 읽어 재저장하는 작업 절대 금지** (PS 5.1이 CP949로 오독 → 이중 인코딩 손상, 커밋 50337cb 실서비스 흰 화면 사고 → f440f17로 복구). 텍스트 치환은 반드시 Bash 도구 `sed` 사용. 손상 시 `git checkout <정상 커밋> -- <파일>` 후 재적용.
- **로컬 미리보기**: `_devserver.ps1` → http://localhost:8899 (mime 맵에 svg/png/jpg 포함 — SVG는 content-type 없으면 이미지 디코드 실패). 검증은 목 API(`Api.xxx = () => Promise.resolve(...)`) 오버라이드 + 브라우저 콘솔. 이 세션 스크린샷 캡처가 타임아웃일 땐 DOM/computed style 검사로 검증.
- **DB 값 전부 한글** (등급/자산/방향/상태/역할). 시간 KST 벽시계값, 단 `guild_transactions.created_at`은 timestamptz(UTC).
- **이미지**: Storage 버킷 `screenshots`. members 목록에서 이미지 블랍 select 금지(유무 플래그만, 개별 조회).

## 절대 규칙

- **커밋/푸시는 반드시 사용자 확인 후에만.** 푸시 = 실서비스 배포.
- **참여점수 계산 로직 변경 금지**: `recalc_participation_scores` RPC 공식 원본 유지. 점수는 로그에서 재계산되는 파생값 — **직접 UPDATE 금지**(다음 재계산에 씻겨나감).
- **참여점수 보정은 "기존 세션의 참석 명단 조정"으로만**: 로그 관리 탭에서 참석자 추가/제거(관리자 전용, 현재 시즌만). 가짜 세션 생성 금지(전체 세션 수=참여율 분모가 늘어 전원 참여율 왜곡). 추가 = 횟수+1/점수+100, 서버가 즉시 재계산 + total_participants 동기화.
- **쟁지원조 명칭**: 결사원 화면(내정보/대시보드 위젯)은 "쟁지원조", 데이터·로그 태그(`!긴급`)·참여율 관리 컬럼은 "긴급" 유지. 데이 09:00:00~17:59:59 / 나이트 18:00~08:59:59. 조 최초 선택=현재 시즌 즉시, **변경=다음 시즌부터**(실수로 바꿔도 이번 시즌은 기존 조 — effective_season 이력 기반).
- **공금(다이아/현금) 정산 규칙**: 다이아 = 룻자(운영진) 계좌로만(선택제, 미선택 시 승인 차단). 별칭 `app_settings.dia_account_alias={"크앙":"admin"}`(곰형→관리자). 현금 = 무조건 결사 금고. 공금 "이력 삭제" = 잔액 역전 취소 기능(기록 청소 아님).
- **시안 = 디자인 명세** (색상/레이아웃/인터랙션 그대로, 데이터 로직 변경은 명시된 것만):
  - 전체 구조: `고니\결사관리_확정구조_시안_v5.html` · 대시보드: `guild-web\대시보드_최종통합_시안_v2.html`
  - 아퀴 목록: `고니\아퀴정보목록_개선시안_v2.html` · 공금: `고니\공금관리_개선시안.html`
  - 로그인: `고니\로그인화면_개선시안_v4.html` · 내정보: `고니\내정보탭_개선시안_v3.html` · 모바일: `고니\모바일_개선시안_단일_v2.html`
- **아퀴 매핑 단일 출처**: `js/gamedata.js`(원본 app.py AQUI_ITEMS 이식본). 신화 아퀴룬 직업별 매핑도 gamedata.js MYTHIC_RUNES 참조(야만투사 2번 없음). 새 매핑 테이블 생성 금지.
- **아이템 구분 5분류 단일 출처**: 아퀴룬 | 브로치 | 별빛심연석 | 찬란한심연석 | 전파편 및 기타 — `GameData.category5(name, category, grade)` (서버 distribution classifyTab 동일 규칙, 구 값은 이름 기반 환산). DB category는 2026-07-21 5분류로 일괄 마이그레이션 완료. 자격 판정·분배 시 아퀴 자동 갱신은 "아퀴"(구)·"아퀴룬"(신) 동일 취급 — 자격 규칙 자체는 원본 불변.
- **심연석 3종(별빛 심연석/조각/찬란한 심연석)은 수량·재고 개념 없음**: `GameData.isOpenApplyItem` — 신청 수량 1 고정(서버도 재고 상한 검증 생략, 찬란한 1인 3개 상한 소멸), ×N 표기 없음, 선정은 운영진이 분배 현황 신청자 명단에서 직접 확정. 목록 노출은 재고 행 기반(행이 있으면 수량 0이어도 신청 가능). 룻자·룻일자는 결사원 화면 비노출(운영진 전용).
- **분배 규정 = 합병 개정판 (2026-07-22)**: 신청 자격은 공통 관문 둘뿐 — ① 스샷 2종 등록 ② 참여율 `apply_min_participation_pct`(기본 35%) 이상. 품목별 조건(전투력/개별 참여율/절대자 풀셋)·유찰 자격 면제 전부 폐지(유찰은 "지망 칸 없는 자유 신청 상태"만 유지, 자유 신청·대량 소모품에도 35% 관문 적용). 운영진 예외(자격 미달 신청 가능)는 유지. 절대자 풀셋 판정/장비 파싱 함수는 제거됨(자격 검사 외 용도 없음). 구 규정 키(min_power류)는 DB에 남아 있어도 무시됨. **내판가(표시 전용)**: 규정 키 `price_krw_legend_aqui`(100,000)/`price_krw_starlight`(50,000) — 전설 아퀴/별빛 심연석 행과 운영진 확정 화면에 표기, 공금 자동 기록 없음.
- **신화 등급만 분배 신청 제외**: 신청 탭 목록 미표시(프론트 필터) + 서버 POST 400 차단. 결사 창고 배지 "문의" + 클릭 토스트. **전설 심연석은 규정 개정(2026-07-28)으로 신청 허용** — 심연석 계열 전체(별빛/조각/찬란한/전설, `isOpenApplyItem`/`isFreeApplyItem` = `심연석` 포함 여부)가 자유 신청: 지망 칸 미사용, 수량 1 고정, 회차당 1개 제한 미적용, 운영진이 신청자 중 선정. 내판가: 전설 아퀴 10만 / 별빛 5만 / 전설 심연석 5천(`price_krw_legend_sim`). 시즌당 1인 10개 한도는 시스템 미강제(운영진 선정 시 관리). `isLegendSimyeonItem`은 내판가 구분용으로만 잔존.
- **결사 창고 = 분배 상위 탭의 첫 하위 화면** (`js/warehouse.js`, 시안 `고니\결사창고_시안_v8.html`): 열람 중심 타일(품명 주인공·등급 칩·×N·신화 금빛·NEW 점·문의/연합 배지만), 룻자 등 관리 요소 절대 비노출. 분배 신청 기간 설정 폼(운영진)도 이 탭 담당. 클릭 = 기간 중이면 신청 탭 점프(`Distribution.focusItem`), 아니면 안내 토스트.
- **분배 신청 = 지망제** (0011 마이그레이션 + 시안 `고니\분배신청_지망제_시안.html`, 2026-07-21 적용): 회차당 최대 3개를 1·2·3순위 지망(`item_requests.wish_rank`, NULL=자유 신청, 대기 상태 한정 user×rank 유니크). 선정은 `close_distribution_period` 3패스(1→2→3순위 풀, 풀 안은 기여점수 DESC·신청일 ASC) — 확정 즉시 잔여 지망 자동 취소(R3, 수동 confirm도 동일), 순위 밖 반려, 유찰은 확정 0건 전설아퀴만 +1. **자유 신청**(`isFreeApplyItem` = free_apply 플래그 ∥ 심연석 3종 ∥ 전설아퀴 2회 유찰)은 지망 칸·R3 미적용, 기존 기여점수순 확정. 신청 RPC는 `add_item_wish_safe`(같은 순위 교체/같은 아이템 순위 이동 자동 처리, 구 `add_item_request_safe`는 미사용 존치). 자격 검사(R6)·finalize·공금·분배취소는 불변. 카테고리 아이템 preference_1/2 화면 용어 = "희망 옵션 1·2".
- **모바일 문법** (767px 이하, 데스크톱 불변): 상단 탭 한 줄 스크롤+sticky+축약(`.lf` 풀네임/`.ls` 축약 스팬), 필터 칩 한 줄 스크롤+0건 숨김, 목록 행은 `.irow.two` + `.r1`/`.r2` 래퍼(데스크톱은 `display:contents`로 투명 → 컬럼 레이아웃 유지), 표는 가로 스크롤+첫 컬럼 sticky. 새 목록 화면을 만들 땐 이 문법을 따를 것.
- **직업 엠블럼**: `img/classes/*.svg`(넥슨 공식, 단색) + `GameData.classEmblemEl(직업, 크기, "dark")` — 밝은 배경엔 "dark" variant. 내정보 아바타/대시보드 카드/결사원 목록/직업 분포에 적용됨.

## 완료된 커밋 (배포됨)

| 커밋 | 내용 |
|---|---|
| ~94b26a9 | 1~9단계 전체 이식 (로그인~분배 후반부) — 이전 CLAUDE.md 이력 참조 |
| 03d528f, 2249347 | 분배 다이아 룻자 선택제 + 곰형→관리자 별칭 (0010) |
| 1e39fe8 | 공금 관리 UI 개편 (KPI 배지/비례 막대/통합 폼/자산×유형 필터/월별 요약/20건 페이지네이션 — 이력은 서버 limit 100) |
| 60e4c1d | 로그인 화면 개편 (론도 성채 배경 img/login_bg.jpg + 유리 카드 + bi_simbol 엠블럼) |
| d399639, bbbfccc | 직업 엠블럼 SVG 7종 + 전 화면 확장 + 헤더 로고/파비콘 |
| 6d23dcb | 내정보 탭 개편 (요약 카드/아퀴 3단 순환 칩/장비 등급색+미리보기/인증샷 드롭존) + 쟁지원조 명칭 |
| 50337cb→f440f17 | 인코딩 손상 사고 및 복구 (위 절대 규칙 참조) |
| e4a0f5f | 공금 이력 모바일 카드형 + 카테고리 칩 건수 배지 + 결사원 관리 [목록\|가입 신청 N] 하위탭 |
| bb64e3b | 모바일(<768px) 전면 보완 |
| 6eaf806 | 관리자 로그 관리 탭 + 참석 보정 + 시즌별 참여 기록 |
| 7576c4f | 분배 신청 탭 단순화 + 카테고리 5분류 표준화 (v=44) |
| 943ca02 | 결사원 목록 정렬 셀렉트(참여점수/기여점수/전투력 순) (v=45) |
| c8beb64 | 참여점수 정렬에 시즌 범위 합산 선택 추가 (v=46) |
| 9d45d85 | 분배 신청 지망제 개편 (0011 + inventory/distribution 함수 재배포) (v=47) |
| 6c72a73 | 지망제 v2 화면(4스텝/다크 지망 카드) + 결사 창고(분배 첫 하위탭, 시안 v8) + 신화 분배 제외 (v=48) |
| e9567f6 | 결사 창고 안내 문구 다크 배너 강조 (v=49) |
| 9abc050 | 결사 합병 준비: guilds + 결사 선택형 + 대량 가입 대비 (0012, v=50) |
| 0923e3a | 분배 규정 합병 개정판(자격 = 스샷+참여율 35% 단일 관문, 내판가) + 규정 탭(열람 전용, 규정탭_시안_v4 — 정적, 개정 시 index.html s-rules 직접 수정) (v=51) |
| 83f4cbd | 합병 초기화 마이그레이션 기록(0013~0016) + 전설 심연석 신청 허용(자유 신청, 내판가 5천, distribution 함수 재배포) + 규정 카드 개정(신화 PVP 1:1, 심연석 "분배 기간에 신청", 주둔지 카드) (v=52) |
| d375111 | 미접속 30분 자동 로그아웃 (auth.js `AUTO_LOGOUT_MS` 상수, last_active_at 하트비트 30초, 부팅 시 만료 검사 → 서버 세션 무효화+로컬 정리; 기록 없는 기존 세션은 첫 접속부터 기록 시작) (v=53) |
| fe1127d | 성능 최적화: Edge Function 병렬화(view=items 9회 순차→3웨이브, status/confirmed/POST/members 목록), status에서 equipment_info 제외, 경량 `view=period`(창고용), 분배 하위탭·결사원 탭 캐시(`DistSync` 버전 버스 — 변경 액션이 bump로 무효화, 가입 신청 하위탭은 캐시 제외), DOM 일괄 삽입(DocumentFragment) (v=54, distribution·members 함수 재배포) |
| ac2d932 | 가입 스샷 첨부 피드백("N장 첨부됨"/실패 사유를 첨부칸 아래 표시, HEIC·인앱 브라우저 안내) + 진단 로그(0017 `client_diag_logs`, register `action=diag` 무인증 — 합병 기간 임시) (v=55, register 함수 재배포) |
| 52232b0 | 내정보 소속결사 셀프 변경 허용 — pfGuild input→guilds 드롭다운(구 값 보존), 저장 patch에 guild_name (profile 함수 재배포 불필요). 합병 소속 매핑을 셀프서비스로 대체 (v=56) |
| 6986521 | 대시보드 결사원 표: # 순번 열 + 결사 배지(닉네임 앞 고정폭 56px 정렬) + 표 안 세로 스크롤 30명(`.dash-scroll`, th sticky) · 결사원 관리: 관리자 전용 결사 배지 클릭 즉시 변경(quickGuildEdit) (v=57) |
| (최신) | 폰 스샷 업로드 수정: ImageUtil에 FileReader 폴백(카카오톡 인앱 웹뷰 objectURL 실패 — 진단 로그로 확인된 실제 사례 대응, 가입·내정보 공용) + 내정보 addPending type 빈 값 수용(안드로이드 선택기)·업로드 중 버튼 표시·HEIC 안내·진단 로그(profile_attach/profile_upload, user_id 포함) — **현재 배포 버전 (v=58)** |

Edge Functions(12개): login, logout, change-password, inventory, item-master, members, treasury, dashboard, distribution, participation, profile, register.

## 진행 중

없음 — 로컬 = 배포 상태 일치.

## 다음 할 일

- **재고 정리**: 사용자가 직접 예정 (별빛 심연석 등 이상값). 대량 정리 필요 시 SQL 지원.
- **실전 분배 E2E**: 다음 분배 기간에 소량으로 신청→확정→승인(다이아+룻자/현금)→공금 확인→분배취소 원복 검증.
- **가입 E2E**: 가입 신청→승인(결사원 관리의 가입 신청 하위탭)→새 계정 로그인→스샷 업로드(Storage 버킷 자동 생성 실전 확인).
- **미뤄둔 부가 기능**: 엑셀 업로드(결사원/참여율), 과거 닉네임 등록 UI, 계좌 DB, 전시즌 이월 블렌딩, 규정 편집 화면.
- 참고: 공금 이력 서버 limit 100 (거래 누적 시 상향 필요), Supabase/Neon 비밀번호 로테이션 미확인, 고니 저장소 CLAUDE.md 커밋(77490c2)은 로컬만(푸시 시 Streamlit 재배포).
